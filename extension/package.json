{
  "name": "@bro/extension",
  "version": "1.0.1",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "reload": "vite build && curl -sS -X POST http://127.0.0.1:3500/reload-extension"
  },
  "dependencies": {
    "@bro/shared": "file:../packages/shared",
    "vite": "^8.2.2"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.7",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
