{
  "name": "@bro/extension",
  "version": "0.2.1",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@bro/shared": "file:../packages/shared",
    "vite": "^8.0.16"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.43",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
